/** Shared field styles so add/edit modals match app chrome (not raw browser inputs). */
export const fieldClassName =
  'h-9 w-full rounded-lg border border-border bg-card px-3 text-sm shadow-xs transition-colors placeholder:text-muted-foreground focus-visible:border-gold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring disabled:cursor-not-allowed disabled:opacity-50'

export const labelClassName = 'text-sm font-medium text-foreground'
